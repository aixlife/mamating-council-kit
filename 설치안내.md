# AI 카운슬 킷 — 마마팅 hq 이식용

만든 날 2026-09-01 · 원본: github.com/aixlife/council-cli (MIT) · 원본 그대로, 수정 없음

## 이게 뭔가

같은 질문을 여러 AI에게 동시에 던져 답을 나란히 받아보는 도구입니다.
API 키가 필요 없고, 이미 로그인해서 쓰는 AI 프로그램들을 그대로 부릅니다.
설치 안 된 AI는 알아서 건너뛰고, 깔려 있는 것들만 소집됩니다.
승자를 정해주지 않습니다 — 서로 다른 의견을 보는 게 목적이고, 결정은 사람이 합니다.

## 어디에 넣나

- `council.mjs` 파일을 hq의 `업무자동화/카운슬/` 폴더에 넣는다
  (규칙 파일이므로 브랜치로 올려 대표님 확인 후 합침)
- 아래 규칙 블록을 hq 루트 CLAUDE.md의 스킬 표 근처에 붙인다

## 루트 CLAUDE.md에 붙일 규칙 블록 (복사용)

```markdown
## AI 카운슬 — 다른 AI 의견 같이 받기

- 트리거: "카운슬 불러줘" / "다른 AI 의견도 받아줘"
- 하는 일: 지금 다루는 질문을 그대로 `node 업무자동화/카운슬/council.mjs "질문"` 으로 실행하고,
  결과 폴더(council-runs/)에 저장된 답들을 읽어 같은 점·다른 점·주의할 점만 정리해 보여준다
- 언제 쓰나: 원고 방향이 갈릴 때, 키워드·제목을 고를 때, 기준을 새로 정하기 전에
- 지킬 것:
  - 다른 AI 프로그램이 설치된 컴퓨터에서만 된다. 웹 클로드 세션에서는 안 된다
  - 고객사 이름·연락처·매출 같은 정보는 질문에 넣지 않는다. 상황만 익명으로 요약해 넣는다
  - 카운슬은 의견만 준다. 어느 쪽을 따를지는 사람이 정한다
```

## 동작 조건 (중요)

- 클로드 말고 다른 AI 프로그램이 최소 하나 더 설치·로그인된 컴퓨터에서만 의미가 있다.
  지금은 팀장님 로컬이 후보다.
- 웹에서 여는 클로드 세션은 매번 새 컴퓨터라 다른 AI가 없어서 안 된다.
- 다른 AI 구독이 생기면 설치만 하면 자동으로 패널에 들어온다. 설정 변경이 필요 없다.
